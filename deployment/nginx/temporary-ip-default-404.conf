# Install only when this host has no existing port-80 default server. It keeps
# an unassigned IP root neutral while named applications add their own host or
# path locations. A later project may replace this generic default explicitly.
server {
    listen 80 default_server;
    server_name _;
    return 404;
}

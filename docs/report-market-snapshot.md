# 报告行情快照

报告发布快照按 `report_id + sector_key + revision_number` 唯一，保存 `market_as_of_date`、行情、指标、来源、EOD 状态、生成时间和哈希。首次固化幂等；后续市场刷新不能覆盖历史报告快照。

发布后修正必须生成增强 revision。撤回不删除已固化证据。板块详情明确区分：

- “本报告发布时”：不可变报告快照；
- “当前最新”：最近一次管理员手工刷新后的完整 EOD。

恒生科技不要求行情快照；行情缺失仍可发布，但必须显示“行情辅助数据未附加”。
